import styled from 'styled-components';
import Tab from '@material-ui/core/Tab';

const StyledNavItem = styled(Tab)`
  color: var(--jet);
`;

const StyledWrapper = styled.div`
  box-shadow: 0 0 5px;
`;

export { StyledNavItem, StyledWrapper };
